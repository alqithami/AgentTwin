"""End-to-end reproduction pipeline for AgentTwin.

This package provides a single entry point to:
- Train controllers + scheduler (multi-seed)
- Evaluate across the paper scenarios (S1--S5)
- Generate CSV/LaTeX tables and B/W figures

See `pipeline/reproduce.py`.
"""
